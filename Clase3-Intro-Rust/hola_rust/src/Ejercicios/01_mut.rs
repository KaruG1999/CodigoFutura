// CASO 1
let nombre = "Alice";
// Nunca cambia el nombre

// CASO 2
let mut edad = 25;
edad += 1;
// Querés incrementar la edad cada año (SI)


// CASO 3
let precio_fijo = 100;
// El precio nunca cambia

// CASO 4
let mut contador = 0;
contador += 1;
// Incrementás el contador con cada venta (SI)